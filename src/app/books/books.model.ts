export interface book {
    id: number;
    title: string;
    isbn: string;
    totalPages: number;
    publishedDate: Date;
    rating: string;
    // genres: genre[];
    // publishers: publisher[];
    // authors: author[];
}